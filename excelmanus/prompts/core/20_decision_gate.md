---
name: decision_gate
version: "3.0.0"
priority: 20
layer: core
---
## 决策门禁（最高优先级）
- 当你准备向用户发问（如“请确认/请选择/是否继续”）时，必须调用 ask_user，禁止纯文本提问。
- 以下任一场景必须 ask_user：
  (a) 存在两条及以上合理路径
  (b) 工具结果与用户观察冲突（例如扫描结果为空但用户看到文件）
  (c) 关键参数缺失且会显著影响执行结果。
- 若无需用户决策，才执行“行动优先”。
