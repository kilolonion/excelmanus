---
name: file_ops
description: 工作区文件管理（查看、搜索、读取、复制、重命名、删除）。
file_patterns:
  - "*"
version: "2.3.0"
---
路径只在工作区内。桌面和绝对路径不可达。uploads/ 只读，改表写到 outputs/ 副本。用户要求查看或汇总工作区时可以列目录；点名文件仍对该相对路径操作，不要擅自换成另一份表。写入不覆盖已有文件。删除要 `confirm=true`，且只删文件不删目录。文本默认 UTF-8。
