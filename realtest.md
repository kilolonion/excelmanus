### 基础读取类

1. 读取"销售明细"工作表的前 10 行数据，告诉我有哪些列
2. 这个文件有多少个工作表？每个工作表分别有多少行数据？
3. 读取"员工花名册"中所有部门为"技术部"的员工姓名和职级

### 数据分析类

4. 统计"销售明细"中每个城市的订单数量和总销售额，按总销售额降序排列
5. 分析"销售明细"中各产品类别的平均折扣率，找出折扣最高的产品
6. 计算"员工花名册"中各部门的平均工资、最高工资、最低工资
7. 找出"月度汇总"中销售额环比增长最快的 3 个月份

### 格式化与样式类

8. 在"销售明细"中，将所有"已取消"状态的行整行标红加粗
9. 给"财务报表"的净利润行添加双线下边框，字体改为 16 号红色
10. 将"考勤表"中所有缺勤（×）的单元格背景改为深红色、白色字体

### 图表生成类

11. 基于"城市分析"数据，生成一个横向柱状图展示各城市客户满意度
12. 基于"月度汇总"数据，生成 2024 年各产品的堆叠柱状图
13. 基于"KPI仪表盘"的月度营收数据，生成带趋势线的折线图

### 公式与计算类

14. 在"销售明细"末尾新增一列"利润估算"，公式为：实付金额 × 30%
15. 在"员工花名册"新增一列"工龄"，用当前日期减去入职日期，以年为单位
16. 在"月度汇总"底部添加年度小计行，分别汇总 2022/2023/2024/2025 四年的数据

### 跨表操作类

17. 从"销售明细"中提取每个产品的总销售额，写入"产品目录"的新列中
18. 将"员工花名册"中各部门人数统计结果，写入一个新工作表"部门统计"
19. 基于"考勤表"的出勤数据和"员工花名册"的工资数据，计算每人的日薪和当月应发工资

### 高难度综合类

20. 创建一个新的"年度总结"工作表，自动从各表汇总：总营收（财务报表）、总订单数（销售明细）、员工总数（花名册）、产品种类数（产品目录），做成仪表盘样式
21. 将"多维矩阵"的三维数据（产品×地区×季度）转换为一维明细表（每行一条记录），写入新工作表
22. 分析整个文件的所有工作表，生成一份"数据质量报告"：每个表的空值数量、重复行数、数据类型分布


### 边框与线条

1. 给"财务报表"的每个大类（营业收入、营业成本、毛利润等）之间添加双线分隔边框，小类用细线
2. 给"月度汇总"按年份分组，每年的最后一行（12月）底部加粗蓝色边框作为年度分隔线
3. 给"考勤表"的周末列（所有标"休"的列）添加左右双线边框，与工作日视觉分离

### 条件格式

4. 在"销售明细"的单价列添加三色色阶：低价绿色 → 中价黄色 → 高价红色
5. 给"员工花名册"的绩效系数列添加图标集：≥1.2 绿色上箭头，0.8-1.2 黄色横线，<0.8 红色下箭头
6. 在"系统日志"的耗时列添加数据条，并且让 FATAL 级别的整行背景变为深红色白字
7. 给"考勤表"添加条件格式：如果某人当月缺勤≥3天，其姓名单元格自动变红色加粗

### 合并单元格与布局

8. 在"城市分析"表顶部插入一个 3 行的报表头区域：第 1 行公司名（合并全宽、居中、24号字），第 2 行报表标题（18号），第 3 行生成日期（右对齐、灰色斜体）
9. 将"多维矩阵"的年份标题行改为跨全宽合并、渐变蓝色背景、白色 14 号字居中
10. 在"KPI仪表盘"的 6 个 KPI 卡片周围添加圆角效果（用粗边框+浅灰背景模拟卡片阴影）

### 字体与填充

11. 给"产品目录"按类别分组着色：每个类别用不同的浅色背景（笔记本蓝、手机绿、平板黄、耳机紫等），类别首行加粗 12 号字
12. 将"财务报表"中所有负数改为红色括号格式，如 `(1,234.56元)`，正数保持黑色
13. 给"数据类型大全"的每个分区标题行设置不同的渐变色主题（数字蓝、日期绿、文本橙、布尔紫）

### 数字格式

14. 将"销售明细"的金额列统一改为"万元"显示，保留两位小数，如 `12.35万`
15. 给"员工花名册"的手机号列设置格式为 `***-****-1234`（隐藏前 7 位）
16. 将"月度汇总"中的环比增长列格式改为：正数显示绿色 `+12.3%↑`，负数显示红色 `-5.6%↓`

### 打印与页面布局

17. 给"销售明细"设置打印区域（A1:L2004），横向打印，缩放到页面宽度，每页重复表头行
18. 给"考勤表"设置 A3 纸张横向打印，页眉显示"XX公司2025年1月考勤表"，页脚显示页码

### 综合样式挑战

19. 创建一个全新的"封面"工作表放在最前面：公司 Logo 占位区（合并 A1:D6 深蓝背景）、报表名称（合并居中 28 号白色粗体）、副标题、生成信息、机密等级标记（红色边框红字"内部机密"），整体模拟正式报告封面
20. 把"KPI仪表盘"改造成真正的仪表盘风格：深色背景（#1B2631），所有文字改白色/浅灰，KPI 卡片用深灰卡片+亮色数字，图表区域用深色主题，模拟 BI 工具的暗色仪表盘效果

---

